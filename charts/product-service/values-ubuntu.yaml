replicaCount: 1

image:
  repository: ghcr.io/matheus06/product-service
  tag: "dev-e4ed65a"

service:
  type: ClusterIP

ingress:
  enabled: true
  className: nginx
  hosts:
    - host: product.local
      paths:
        - path: /
          pathType: Prefix

resources:
  limits:
    cpu: 200m
    memory: 128Mi
  requests:
    cpu: 50m
    memory: 64Mi

env:
  - name: ASPNETCORE_ENVIRONMENT
    value: "Development"
  - name: OTEL_EXPORTER_OTLP_ENDPOINT
    value: "http://otel-collector:4317"

metrics:
  enabled: false
